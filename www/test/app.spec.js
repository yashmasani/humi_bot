// mock
const helper = require('../app/helper');

describe('Run Time Off Events', function() {
  describe('run time off events works', function() {
    it('works', function(done) {
     console.log(helper);
     done();
    });
  });
});
